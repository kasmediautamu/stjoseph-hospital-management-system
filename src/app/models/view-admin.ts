export interface ViewAdmin {
  id?: number;
  viewName: string;
  viewDescription: string;
  auditor: string;
  followUp: string;
  status: string;
  accountAge: string;
  hiddenRecords: string;
  defaultView: boolean;
}
